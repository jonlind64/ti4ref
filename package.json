{
  "name": "ti4-quick-guide",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/make-php-entry.js",
    "lint": "tsc --noEmit",
    "test": "node --test tests/*.test.js",
    "import:rules": "python3 scripts/import_rules.py",
    "validate": "node scripts/validate-data.js"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
